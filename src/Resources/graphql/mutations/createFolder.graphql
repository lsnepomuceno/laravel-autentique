mutation createFolder($folder: FolderInput!, $parent_id: UUID, $type: FolderTypeEnum) {
  createFolder(folder: $folder, parent_id: $parent_id, type: $type) {
    ...FolderFields
    parentFolder {
      id
      name
    }
  }
}
